import styled from "styled-components";

const Container = styled.div`
  padding: 10%;
  /* padding: env(safe-area-inset-top, 4vh) env(safe-area-inset-right, 4vw)
    env(safe-area-inset-bottom, 4vh) env(safe-area-inset-left, 4vw); */
`;

export default Container;
